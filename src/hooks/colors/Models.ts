export interface BaseColors {
  white: string;
  black: string;
  red: string;
  gray: string;
  blue: string;
  orange: string;
  green: string;
  yellow: string;
}

export interface Colors extends BaseColors {
  background: string;
  font: string;
  primary: string;
  disabled: string;
}
